import ComingSoon from "@/components/ComingSoon";

export const metadata = { title: "Nasheed — NUR" };

export default function NasheedsPage() {
  return (
    <ComingSoon
      title="Nasheed"
      body="The members-only library — streamed in the app, never downloadable."
      blockedOn="Waiting on the audio files. The player and the signed-URL gating are specified and ready to build the moment the tracks arrive."
    />
  );
}
